import Topbar from "@/components/Topbar/Topbar";

const ProblemPage = () => {
    return (
        <div>
            <Topbar problemPage={ true } />
        </div>
    );
}

export default ProblemPage;